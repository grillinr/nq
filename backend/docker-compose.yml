services:
  neo4j:
    image: neo4j:latest
    ports:
      - "7474:7474"   # Neo4j browser UI
      - "7687:7687"   # Bolt protocol (used by the Go driver)
    environment:
      NEO4J_AUTH: neo4j/testpass
    volumes:
      - neo4j_data:/data

volumes:
  neo4j_data:
